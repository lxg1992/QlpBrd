const initialState = {
  text: "",
  visible: false,
  category: null,
};
export default function alertReducer(state = initialState, action = null) {
  switch (action.type) {
    default:
      return state;
  }
}
